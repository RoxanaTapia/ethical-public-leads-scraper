# =============================================
# Ethical Public Leads Scraper - Environment Template
# =============================================
# Keep values empty by default. Fill only when enabling AI enrichment in src/extractor.py.
# The Streamlit app reads these via python-dotenv when enriching leads (optional).

# Generic LLM configuration (provider-agnostic)
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=
